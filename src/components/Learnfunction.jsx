import { useState } from "react";

const Learnfunction = () => {
  const [name, setName] = useState(false);
  this.abc = { sachin: "jakhal" };
  return (
    <div>
      <p>{this.state.name}</p>
      <button onClick={() => setName(!name)}>Change Name</button>
    </div>
  );
};

export default Learnfunction;
